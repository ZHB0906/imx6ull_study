# /etc/wpa_supplicant.conf —— 默认不含任何密码
#
# 用 /root/set-wifi.sh 生成（推荐，密码只以 psk 哈希落盘）：
#     /root/set-wifi.sh "你的WiFi名" "你的密码"
#
# 或者手工加 network 块后 /etc/init.d/S45wifi restart
ctrl_interface=/var/run/wpa_supplicant
update_config=1
country=CN

# network={
# 	ssid="你的WiFi名"
# 	psk="你的密码"
# }
